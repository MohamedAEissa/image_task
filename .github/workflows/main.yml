
on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "login to docker hub "
        run: docker login --username mohamedeisa12 --password dckr_pat_7tArfu_H7wpOBedOfDnzpn3RPbA
    
